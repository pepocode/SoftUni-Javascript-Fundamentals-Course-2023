function solve(pagesNumber, pagesPerHour, days){
    let totalTime = pagesNumber / pagesPerHour;
    let requiredHoursPerDay = totalTime / days;
    console.log(requiredHoursPerDay)
}
solve(212,
    20 ,
    2 
    );